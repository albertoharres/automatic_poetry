void because() {
 
 int posCj, posN, posV, posO, posart, pospro, posA0, posAdv;
 
 
 Nx = int(random(0, noun.length)); // Noun
 
 String Cj = "because "; 
 String N = noun[int(random(0, noun.length))]; // Noun 
 
 if(past == true) { 
  V = "was "; 
 } else if(present == true) {
  V = "is ";
 }else { 
  V = "would be"; 
 }
  
}
